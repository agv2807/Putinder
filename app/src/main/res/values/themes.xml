<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Putinder" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/Theme.Putinder.ActionBar</item>
        <item name="actionBarTheme">@style/Widget.AppTheme.ActionBar</item>

    </style>

    <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/splash_background</item>
    </style>

    <style name="Theme.Putinder.ActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="background">@color/white</item>
        <item name="elevation">4dp</item>
    </style>

    <style name="Widget.AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <!-- title text color -->
        <item name="titleTextColor">@color/black</item>
        <!-- subtitle text color -->
        <item name="subtitleTextColor">@color/black</item>
    </style>

</resources>