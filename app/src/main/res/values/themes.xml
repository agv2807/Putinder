<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Putinder" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/Theme.Putinder.ActionBar</item>
        <item name="actionBarTheme">@style/Widget.AppTheme.ActionBar</item>

    </style>

    <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/splash_background</item>
    </style>

    <style name="Theme.Putinder.ActionBar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="background">@color/white</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="Widget.AppTheme.ActionBar" parent="@style/Widget.MaterialComponents.Light.ActionBar.Solid">
        <item name="android:textColor">@color/mint</item>
        <item name="android:textColorPrimary">@color/mint</item>
        <item name="android:titleTextColor">@color/mint</item>
        <item name="android:textColorHint">@color/mint</item>
        <item name="titleTextColor">@color/mint</item>
    </style>

</resources>